#!/bin/sh
# ╔══════════════════════════════════════════════════════════════════════╗
# ║  Star-Talk / 星语 — initramfs/init (MIGRATION NOTICE)              ║
# ╚══════════════════════════════════════════════════════════════════════╝
#
# Star-Talk has been migrated from Linux to NetBSD.
#
# The old initramfs-based init (BusyBox + custom /init) has been
# replaced by NetBSD's native rc.d init system.
#
# New init architecture:
#   - NetBSD kernel boots directly via boot.cfg
#   - /etc/rc handles system initialization
#   - /etc/rc.d/startalk-splash provides AIX-style boot display
#   - /etc/rc.d/tor and /etc/rc.d/i2pd (disabled by default)
#   - SDDM starts KDE Plasma desktop
#
# Hardware detection is now done via:
#   - sysctl (hw.physmem, hw.ncpu, machdep.cpu_brand)
#   - dmesg parsing for GPU, network, audio devices
#
# For build instructions, see: README.md
# For architecture details, see: doc/ARCHITECTURE.md
# For known issues, see: doc/KNOWN_ISSUES.md

echo "Star-Talk has been migrated to NetBSD."
echo "This initramfs directory is retained for reference only."
echo "See doc/ARCHITECTURE.md for the new boot architecture."
exit 1
